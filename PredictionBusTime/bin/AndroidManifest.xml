<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.predictionbustime"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >

            <!-- 鎖定螢幕，portrait是直立 -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SearchRouteByIdLayout"
            android:label="@string/title_activity_search_route_by_id_layout"
            android:screenOrientation="portrait" >

            <!-- 鎖定螢幕，portrait是直立 -->
        </activity>
        <activity
            android:name=".PrintSearchByIdResult"
            android:label="@string/title_activity_print_search_by_id_result"
            android:screenOrientation="portrait" >

            <!-- 鎖定螢幕，portrait是直立 -->
        </activity>
        <activity
            android:name=".SignInActivity"
            android:label="@string/title_activity_sign_in" 
            android:screenOrientation="portrait" >

            <!-- 鎖定螢幕，portrait是直立 -->
        </activity>
        <activity
            android:name=".RegisteredActivity"
            android:label="@string/title_activity_registered" 
            android:screenOrientation="portrait" >

            <!-- 鎖定螢幕，portrait是直立 -->
        </activity>
        <activity
            android:name=".ChangePasswordActivity"
            android:label="@string/title_activity_change_password" 
            android:screenOrientation="portrait" >

            <!-- 鎖定螢幕，portrait是直立 -->
        </activity>
        <activity
            android:name=".ChangePasswordMainActivity"
            android:label="@string/title_activity_change_password_main" 
            android:screenOrientation="portrait" >

            <!-- 鎖定螢幕，portrait是直立 -->
        </activity>
    </application>

</manifest>
